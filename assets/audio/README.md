# assets/audio

Owned by the **Music Designer**. For the M1 demo all audio is generated at runtime
via the Web Audio API (see `src/js/engine/audio.js`) — no bundled audio files, no
licensing questions. Whether to add real music/SFX is Open Question Q4 in
`docs/decisions.md`.
